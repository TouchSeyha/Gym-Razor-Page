@page
@model Gym.Pages.GymHalls.DetailsModel

@{
    ViewData["Title"] = "Gym Hall Details";
    ViewData["Subtitle"] = "View gym hall information";
}

<div class="row mb-4">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header bg-light d-flex justify-content-between align-items-center">
                <h5 class="card-title mb-0">
                    <i class="bi bi-building me-2"></i>@Model.GymHall.HallName
                </h5>
            </div>
            <div class="card-body">
                <div class="row mb-4">
                    <div class="col-md-3 text-center mb-3 mb-md-0">
                        <div class="avatar-lg bg-primary text-white rounded-circle d-flex align-items-center justify-content-center mx-auto mb-3">
                            @Model.GymHall.HallName.Substring(0, 1)
                        </div>
                        <h5 class="mb-1">@Model.GymHall.HallName</h5>
                        <p class="text-muted mb-0">@Model.GymHall.Location</p>
                    </div>
                    <div class="col-md-9">
                        <div class="row g-0">
                            <div class="col-md-6 mb-3">
                                <strong><i class="bi bi-people me-2 text-muted"></i>Capacity</strong>
                                <div class="mt-1">@Html.DisplayFor(m => m.GymHall.Capacity)</div>
                            </div>
                            <div class="col-md-6 mb-3">
                                <strong><i class="bi bi-geo-alt me-2 text-muted"></i>Location</strong>
                                <div class="mt-1">@Html.DisplayFor(m => m.GymHall.Location)</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card-footer bg-light">
                <div class="d-flex justify-content-between">
                    <a asp-page="./Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i> Back to List
                    </a>
                    <a asp-page="./Edit" asp-route-id="@Model.GymHall.Id" class="btn btn-warning">
                        <i class="bi bi-pencil me-1"></i> Edit
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .avatar-lg {
        width: 100px;
        height: 100px;
        font-size: 40px;
    }
</style>
